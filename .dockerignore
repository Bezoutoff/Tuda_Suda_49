# ==============================================================================
# .dockerignore - Exclude files from Docker build context
# ==============================================================================
#
# This file reduces the Docker build context size by ~200MB
# and prevents secrets from being accidentally copied into the image.
#
# ==============================================================================

# Node.js
node_modules
npm-debug.log
yarn-error.log
package-lock.json

# Build artifacts
dist
*.js.map

# Logs
logs
*.log
*.csv

# Environment files (NEVER include secrets in image!)
.env
.env.*
!.env.example

# Git
.git
.gitignore
.gitattributes

# Documentation (not needed in runtime image)
*.md
!README.md
docs

# IDE and editor files
.vscode
.idea
*.swp
*.swo
*~
.DS_Store

# Python
__pycache__
*.py[cod]
*$py.class
*.so
.Python
venv
ENV
env

# Systemd (not used in Docker)
systemd

# Claude AI
.claude

# Docker files (don't need docker files inside the image)
Dockerfile
docker-compose.yml
.dockerignore

# CI/CD
.github
.gitlab-ci.yml

# Test files
test
tests
*.test.js
*.test.ts
*.spec.js
*.spec.ts

# Miscellaneous
*.bak
*.tmp
*.temp
.cache
